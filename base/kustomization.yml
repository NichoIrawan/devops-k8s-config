apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Config
  - configmap.yml
  - secret.yml

  # Application
  - web-deployment.yml
  - web-service.yml

  # Database
  - postgres-service.yml
  - postgres-statefulset.yml

  # Networking
  - ingress.yml
